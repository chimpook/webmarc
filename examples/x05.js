"use strict";

/**
 * Example of getstr
 */

$(document).ready(function () {

    x.initscr($("#container"));

    /**
     * TODO: 1)Загрузка текста исходного файла *.c, 2) выделение жирным шрифтом комментариев, 3) вывод постраничный
     *
     */

    x.refresh();
});

var x = new Webcurses('Example 5');

